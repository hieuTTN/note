câu lệnh git1. git init (khởi tạo git)2. git remote remove origin (xóa repo đã kết nối)3. thay đổi tài khoản git trên win:mở control panel -> cridential manager(view small icon) -> tab window cridential -> xóa liên kết git đimở gitbash 1. git config --global user.username <username>2. git config --global user.email <email>3. git config --global user.password <password>// thêm thành viên vào gitvào setting trên github.com/projectvào tab setting chọn collaborators -> add peopleđăng nhập vào gmail tài khoản được add để accept// push code từ branch thành viêngit clone để clone project vềgit checkout <branch name> để chuyển sang nhánh khácgit add -A (add tất cả vào git)git commit -m "name"git push -u origin <tenbranch> git config --local core.ignorecase false  (xóa cache)clone từ 1 nhánhgit clone -b <tên nhánh> <link git project>